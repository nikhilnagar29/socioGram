<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="icon" class="rounded-full" type="image/x-icon" href="/image/favicon.ico">    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body, input, button {
            font-family: 'Inter', sans-serif;
        }
        @keyframes fade-in {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fade-in 0.5s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-800 via-gray-900 to-black text-white flex justify-center items-center min-h-screen">

    <% var ImageName = "https://img.freepik.com/premium-photo/black-logo-with-silver-logo-that-says-silver-it_905510-10632.jpg" %>

    <div class="w-full bg-black max-w-5xl p-8 shadow-2xl rounded-2xl animate-fade-in">
        <div class="flex justify-center mb-6">
            <a href="/" class="block">
                <img class="w-40 h-40 rounded-full  border-blue-600 hover:shadow-lg hover:scale-105 transition-all duration-300" 
                    src="<%=ImageName%>" 
                    alt="Profile Image">
            </a>
        </div>

        <form action="request-otp" method="post">
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-6">
                <div class="flex flex-col max-w-[300px]">
                    <label for="name" class="font-semibold text-white mb-2">Name:</label>
                    <input type="text" id="name" class="w-full p-3 bg-gray-800 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-600 focus:border-transparent focus:bg-gray-700 text-white" placeholder="Enter Name" name="name">
                </div>
                <div class="flex flex-col max-w-[300px]">
                    <label for="email" class="font-semibold text-white mb-2">Email:</label>
                    <input type="email" id="email" class="w-full p-3 bg-gray-800 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-600 focus:border-transparent focus:bg-gray-700 text-white" placeholder="Enter Email" name="email">
                </div>
                <div class="flex flex-col max-w-[300px]">
                    <label for="dob" class="font-semibold text-white mb-2">Date of Birth:</label>
                    <input type="date" id="dob" class="w-full p-3 bg-gray-800 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-600 focus:border-transparent focus:bg-gray-700 text-white" name="dateOfBirth">
                </div>
            </div>

       
            <div class="flex justify-center">
                <button class="px-8 py-3 bg-blue-600 text-white font-bold rounded-full shadow-lg hover:bg-blue-700 active:bg-blue-800 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-opacity-50">
                    Next
                </button>
            </div>
        </form>
    </div>

</body>
</html>