<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write Mail to Developer</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="icon" class="rounded-full" type="image/x-icon" href="/image/favicon.ico">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body, input, button {
            font-family: 'Roboto', sans-serif;
        }
        @keyframes fade-in {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fade-in 0.5s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-800 via-gray-900 to-black text-white flex justify-center items-center min-h-screen">

    <% var ImageName = "https://img.freepik.com/premium-photo/black-logo-with-silver-logo-that-says-silver-it_905510-10632.jpg" %>
    <div class="w-full bg-black max-w-5xl p-8 shadow-2xl rounded-2xl animate-fade-in">
        <div class="flex justify-center mb-6">
            <a href="/home" class="block">
                <img class="w-40 h-40 rounded-full border-blue-600 hover:shadow-lg hover:scale-105 transition-all duration-300" 
                    src="<%= ImageName %>" 
                    alt="Profile Image">
            </a>
        </div>
        
        <form action="/send-email" method="post" class="space-y-4">
            <div class="text-center text-white mb-6">
                <h3 class="text-xl font-bold mb-2">Write Mail to Developer</h3>
                <p class="text-sm text-gray-400">Compose your message below.</p>
            </div>

            <!-- Subject Input -->
            <div class="flex flex-col">
                <label for="subject" class="text-gray-400 mb-1">Subject:</label>
                <input type="text" id="subject" name="subject" 
                    class="w-full p-3 bg-gray-800 text-white border border-blue-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none transition-all ease-in-out duration-200" 
                    placeholder="Enter Subject">
            </div>

            <!-- Body Input -->
            <div class="flex flex-col">
                <label for="body" class="text-gray-400 mb-1">Body:</label>
                <textarea id="body" name="body" rows="5" 
                    class="w-full p-3 bg-gray-800 text-white border border-blue-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none transition-all ease-in-out duration-200" 
                    placeholder="Write your message here..."></textarea>
            </div>

            <div class="flex justify-center">
                <button type="submit" 
                    class="px-8 py-3 bg-blue-600 text-white font-bold rounded-full shadow-lg hover:bg-blue-700 active:bg-blue-800 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-opacity-50">
                    Send Email
                </button>
            </div>
        </form>
    </div>

</body>
</html>