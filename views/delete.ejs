<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Account - SocioGram</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Favicon -->
    <link rel="icon" class="rounded-full" type="image/x-icon" href="/image/favicon.ico">

    <style>
        body, input, button {
            font-family: 'Roboto', sans-serif;
        }

        @keyframes fade-in {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-fade-in {
            animation: fade-in 0.5s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-800 via-gray-900 to-black text-white flex justify-center items-center min-h-screen">

    <% var ImageName = "https://img.freepik.com/premium-photo/black-logo-with-silver-logo-that-says-silver-it_905510-10632.jpg" %>
    <% var userimage = "path/to/user-profile-image.jpg"; %>
    <% var username = "Username"; %>

    <div class="w-full max-w-5xl p-8 shadow-2xl rounded-2xl bg-black animate-fade-in">
        
        <!-- Logo -->
        <a href="/user/home" class="block mb-6">
            <div class="flex items-center hover:scale-105 transition-transform">
                <img class="w-12 h-12 rounded-full  border-blue-600 shadow-lg hover:shadow-xl transition-all duration-300" 
                    src="<%=ImageName%>" 
                    alt="SocioGram Logo">
                <span class="ml-3 text-2xl font-bold text-gray-100">SocioGram</span>
            </div>
        </a>

        <div class="flex flex-col md:flex-row items-start space-y-8 md:space-y-0 md:space-x-10">
            
            <!-- User Profile Section -->
            
            <div class="flex-1 mt-[50px] ml-[25px] mr-[-200px] p-4 flex flex-col items-center justify-center md:items-start">
                <a href="/user/profile">
                    <div>   
                        <img class="w-36 h-36 rounded-full border-4 border-blue-600 object-cover mb-4 shadow-lg hover:scale-105 transition-transform"
                            src="data:<%= user.profileImage.contentType %>;base64,<%= user.profileImage.data.toString('base64') %>" 
                            alt="User Profile">
                    </div>
                    <h2 class="text-xl font-bold text-center md:text-left">@<%=user.username%></h2>
                </a>
            </div>
            

            <!-- Delete Account Section -->
            <div class="flex-1 p-4 space-y-6">
                <h1 class="text-white text-4xl md:text-5xl font-extrabold leading-tight">
                    Delete Account
                </h1>
                <p class="text-xl md:text-2xl font-medium text-gray-300">
                    Are you absolutely sure you want to delete your account?
                </p>

                <p class="text-gray-400">
                    Deleting your account is <span class="font-bold text-red-500">permanent</span> and cannot be undone. All your data, posts, and messages will be erased.
                </p>

                <!-- Action Buttons -->
                <div class="flex flex-col md:flex-row md:space-x-4 space-y-4 md:space-y-0">
                    <!-- Delete Account Button -->
                    <form action="/log/delete/<%=user._id%>" method="POST" onsubmit="return confirm('Are you absolutely sure you want to delete your account? This action cannot be undone.');">
                        <button type="submit" 
                            class="w-full md:w-auto px-8 py-3 bg-red-600 text-white font-bold rounded-full shadow-lg hover:bg-red-700 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-red-600 focus:ring-opacity-50">
                            Delete Account
                        </button>
                    </form>

                    <!-- Cancel Button -->
                    <form action="/user/profile">
                        <button type="button" 
                            onclick="window.location.href='/profile';" 
                            class="w-full md:w-auto px-8 py-3 text-blue-600 border-2 border-blue-600 rounded-full font-bold hover:bg-blue-600 hover:text-white transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-opacity-50">
                            Cancel
                        </button>
                    </form>
                </div>

            </div>

        </div>
    </div>

</body>
</html>
